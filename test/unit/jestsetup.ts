// import Vue from 'vue';
// import { Install } from '@ferrymen/fm-ui-vue';

// Vue.use(Install);


// (HTMLCanvasElement as any).prototype.getContext = () => {
//   return;
// };

// (global as any).CLIENT = true;
// (global as any).SERVER = true;
// (global as any).TEST = true;

// Vue.config.productionTip = false;

// Vue.mixin({
//   created() {

//   },
// });